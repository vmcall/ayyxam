#pragma once
#include <cstdint>
#include <vector>

namespace ayyxam::guard
{
	static std::vector<std::uint32_t> hidden_pid;
	static std::vector<std::wstring> hidden_adapter;
	static std::vector<std::wstring> hidden_window;
}